// add logic so that animation should work only when we clicks button

function upload() {
    const progressbar=document.querySelector('.progress-bar');
    progressbar.setAttribute('id','play-animation');
}